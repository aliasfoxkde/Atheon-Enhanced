name: rails-secret-key-hardcoded
category: rails
match: (secret_key_base|secret_token)\s*=\s*["'][a-f0-9]{64}["']
severity: high
enabled: true
description: Rails secret key or token hardcoded in source. This should be stored
  in environment variables, not committed to version control.
