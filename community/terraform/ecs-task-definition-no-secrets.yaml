name: tf-ecs-no-secrets
category: terraform
match: aws_ecs_task_definition
severity: medium
enabled: true
description: "ECS task definition embeds secrets in environment variables. Use AWS Secrets Manager or Parameter Store instead. Note: disabled due to RE2 incompatibility - needs AST-based detection."
