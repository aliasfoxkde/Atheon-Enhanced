name: concurrent-workflow-cancellation
match: "(?i)concurrency:"
severity: low
enabled: true
description: 'Pattern: concurrent-workflow-cancellation — Detects workflows with concurrency set but missing cancel-in-progress: true. Without this, multiple runs of the same workflow may execute simultaneously wasting resources.'