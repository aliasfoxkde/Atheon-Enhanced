name: vue-template-injection
category: vue
match: :[a-z]+=["']{{[^}]*request|:[a-z]+=["']{{[^}]*input|:[a-z]+=["']{{[^}]*params
severity: high
enabled: true
description: Vue.js template injection detected. User input may be directly interpolated
  into templates without sanitization.
