name: express-sql-injection
category: express
match: (query|execute)\s*\(.*[\+\`].*\$\{
severity: high
enabled: true
description: Express.js SQL query with template literal interpolation detected. This
  can lead to SQL injection vulnerabilities.
