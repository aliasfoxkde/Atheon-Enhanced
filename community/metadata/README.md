# metadata

Patterns in this category:

| Pattern | Description | Severity |
|---------|-------------|----------|
| `backup-file` | Pattern: backup file. Pattern in category metadata. Detects backup files that may contain sensitive data from previous versions. | low |
| `IDE-config-leak` | Pattern: IDE config leak. Pattern in category metadata. Detects IDE configuration files that may contain embedded credentials or settings. | medium |
| `os-cache-file` | Pattern: OS cache file. Pattern in category metadata. Detects operating system cache and metadata files. | low |
| `temporary-file` | Pattern: temporary file. Pattern in category metadata. Detects temporary files that should not be committed to source control. | low |

---
*Auto-generated from pattern YAML files*
