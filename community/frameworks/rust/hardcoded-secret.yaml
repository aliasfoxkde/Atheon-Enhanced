name: rust-hardcoded-secret
category: rust
match: (api_key|apiKey|secret|password|token|credential)\s*[:=]\s*["\'][^"\'\s]{8,}["\']
severity: critical
enabled: true
description: Hardcoded secret detected in Rust code. Secrets should be loaded from environment variables or secure configuration systems.
