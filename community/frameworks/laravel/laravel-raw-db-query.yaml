name: laravel-raw-db-query
category: laravel
match: DB::(?:select|statement|unprepared)\s*\(.*[\"\'].*%s
severity: high
enabled: true
description: Laravel raw database query with string formatting detected. This can
  lead to SQL injection vulnerabilities.
