# terraform

Patterns in this category:

| Pattern | Description | Severity |
|---------|-------------|----------|
| `db-public-access` | Pattern: database public access. Pattern in category terraform. Detects database instances configured with public accessibility. | high |
| `hardcoded-tf-secrets` | Pattern: hardcoded secrets in terraform. Pattern in category terraform. Detects secrets hardcoded directly in terraform configuration files. | critical |
| `s3-public-access` | Pattern: S3 public access. Pattern in category terraform. Detects S3 bucket configurations allowing public access. | high |
| `unencrypted-storage` | Pattern: unencrypted storage. Pattern in category terraform. Detects storage resources configured without encryption at rest. | high |

---
*Auto-generated from pattern YAML files*
