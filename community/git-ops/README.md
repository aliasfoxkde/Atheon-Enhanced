# git-ops

Patterns in this category:

| Pattern | Description | Severity |
|---------|-------------|----------|
| `force-push-detected` | Pattern: force push detected. Pattern in category git-ops. Detects force push commands which can overwrite remote history. | high |
| `git-credential-leak` | Pattern: git credential leak. Pattern in category git-ops. Detects git credentials exposed in scripts or URLs. | critical |
| `protected-branch-delete` | Pattern: protected branch deletion. Pattern in category git-ops. Detects deletion or modification of protected branch settings. | critical |

---
*Auto-generated from pattern YAML files*
